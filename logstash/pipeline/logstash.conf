input {
	rabbitmq {
        host => "rabbitmq"
        port => 5672
        user => "r-mobium"
        password => "JsawWdfk231L"
        prefetch_count => 2
        exchange => "logs"
        queue => "logs.panel"
        durable => true
        passive => false
        ack => true

    }
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
	}
}
